Put third-party source checkouts here.

Expected for the smoke harness:

- `third_party/FunkyDNS` checked out from the private GitHub repo
  `P4X-ng/FunkyDNS`

If your checkout is missing, initialize it with:

```bash
git submodule update --init --recursive third_party/FunkyDNS
```
