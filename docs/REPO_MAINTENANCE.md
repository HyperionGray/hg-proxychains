# Repository maintenance workflow (legacy note)

This page is intentionally minimal. The canonical maintenance documentation now
lives in:

- `docs/REPO-HYGIENE.md`

Use `scripts/repo_hygiene.py` directly for maintenance scans/cleanup, or use
`scripts/repo_maintenance.py` as a compatibility wrapper.
